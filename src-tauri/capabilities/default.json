{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window only receives core Tauri capabilities.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
