# Cursor 额度查看器领域上下文

更新时间：2026-08-30

## 产品目标

Cursor Usage Viewer（Cursor 额度查看器）是一个面向 Windows、macOS、Linux 的非官方开源桌面工具，用于管理多个 Cursor 账号，并由用户主动查询、集中展示各账号的额度数据。

## 核心术语

- **账号**：由 Cockpit Tools JSON 或本机 Cursor 数据导入的一条 Cursor 身份记录。
- **账号摘要**：用于列表、搜索和筛选的非额度字段，包括账号 ID、邮箱、Auth ID、标签、套餐、订阅状态、来源和时间戳。
- **核心额度**：Cursor 账号的 Total、Auto + Composer、API、On-Demand 与计费周期数据，是账号卡片的四组主要额度。
- **Sand 附加状态**：独立于核心额度的 Grok/Sand 用量、访问资格与重置时间；缺失或失败不改变核心额度。
- **额度快照**：某账号最后一次成功取得的核心额度和 Sand 附加状态及其各自更新时间；它用于重启后的本地展示，但不替代用户主动发起的实时查询。
- **账号明细**：每个账号单独落盘的完整 JSON，包含账号摘要、凭据和额度快照。
- **账号索引**：不包含完整额度和凭据的轻量列表文件，用于快速加载账号列表。
- **手动刷新**：用户明确点击单账号、选中账号或全部账号的 Cursor 额度刷新操作；启动应用不会自动执行该操作。
- **高密度账号卡片**：在单张卡片内直接展示身份、套餐、标签、Total、Auto + Composer、API、On-Demand、周期和更新时间，无需进入详情页。
- **应用更新检查**：应用按用户的更新设置访问本项目发布源，判断是否存在更高版本。它不查询 Cursor 额度，也不携带 Cursor 账号凭据。
- **更新资产**：某个应用版本面向特定操作系统、架构和安装方式提供的可下载安装包。只有通过项目更新签名验证的资产才可由应用安装。
- **稳定通道**：面向普通用户的唯一更新通道，只接收已经人工发布的正式版本，不接收草稿、Beta 或 Nightly。
- **草稿发布**：已经汇集构建产物但尚未向普通用户公开、也不会被稳定通道发现的待验收版本。
- **系统托盘**：主窗口关闭后仍可承载应用进程和应用更新检查的桌面入口；它不代表允许后台刷新 Cursor 额度。
- **非官方项目**：由社区独立维护、未获得 Cursor/Anysphere 隶属或背书的项目身份。

## 已确认产品边界

- 主页面采用 Cockpit Tools Cursor 账号页的信息结构：Cockpit 风格的单页外壳、单行工具栏、全选/批量操作区、自适应高密度卡片网格；不再保留旧的“概览 / 账号 / 安全”三页结构。
- 视觉采用已确认的第 3 套深色设计语言；Cockpit 只提供信息层级和交互结构参考，不复制其代码、CSS、品牌、资源或文案。
- 侧栏保留品牌、Cursor 主入口和底部设置入口；工具栏不显示 OAuth 添加“+”、切号、注入或与本项目无关的平台能力。
- 主窗口照抄 Cockpit Tools 的 1280×800 默认尺寸、900×600 最小尺寸、居中和可缩放配置。
- 卡片网格照抄 `repeat(auto-fill, minmax(320px, 1fr))`；可用宽度足够时形成三列，窗口变窄时自动降列，账号较多时允许滚动并沿用 Cockpit 的分页结构。
- 首版常用管理能力包括搜索、套餐与标签筛选、排序、网格/列表切换、多账号粘贴导入、选中/全部刷新和删除本地记录。
- 完整账号 JSON 导出沿用 Cockpit 行为：支持当前筛选范围、选中账号和单账号导出；点击后直接进入默认遮罩的预览，可显隐、复制及保存；内容包含明文 Token。
- 不加入切换 Cursor 当前账号、Token 注入、启动 Cursor、多开、OAuth 或后台自动刷新。
- 启动时可加载已经落盘的账号及最后额度快照，但不得因此自动访问 Cursor 网络端点；应用更新检查可按设置独立访问本项目发布源。
- 本机 Cursor 当前账号只在用户点击“读取本机账号”后只读识别并标记；启动时不自动打开 Cursor 数据库。
- Windows、macOS、Linux 同时属于首发平台；应用只维护稳定更新通道。
- 产品长期只服务 Cursor，不以未来多 Provider 为由提前扩张领域模型。
- 界面首版维护简体中文和英文；公开身份、图标和文案不得暗示官方关联。

## 本地持久化模型

- 按 Cockpit Tools 的逻辑保存：一个轻量账号索引文件，加一个每账号独立的明细目录。
- 账号明细保存 Access Token、Refresh Token、账号资料和最后额度快照；Token 与 Cockpit Tools 一样以明文 JSON 落盘，不额外加密。
- 文件写入使用同目录临时文件替换，并保留 `.bak`；主文件解析失败时尝试备份恢复。
- 索引缺失或损坏时扫描账号明细、去重并重建索引。
- 原始导入 JSON 提交后仍须立即清空输入框；凭据不得返回脱敏 DTO 之外的前端状态、写入日志或发送到非白名单目标。

## 额度数据规则

- Cockpit Tools 导入数据中的 `cursor_usage_raw` 在导入后立即解析并显示，同时明确标记为“导入缓存”及其更新时间，不能冒充本轮实时查询结果。
- 实时查询成功后，映射成稳定的额度快照再落盘。
- 手动刷新时照抄 Cockpit 的 Token 续期规则：Access Token 无法解析或在五分钟内到期时，使用 Refresh Token 请求 Cursor OAuth Token 端点；成功后保存新 Token，失败时继续尝试现有 Access Token。启动和后台不续期。
- 字段缺失保持未知并显示“暂无数据”，不得转换为 0。
- 单个额度端点失败不得抹掉同一账号其他已经成功取得的数据。

## Cockpit 源码直接决定的交互

- 卡片直接展示邮箱、Auth ID、最多两个标签、四组核心额度、紧凑 Sand 附加状态、日期与操作；超出两个标签显示数量。
- 重复身份按 Cockpit 的合并规则处理：保留主记录，补齐空字段，合并并去重标签，保留更早创建时间和更晚使用时间。
- 单个删除和批量删除均先显示确认弹窗。
- 刷新全部沿用 Cockpit 的统一批量刷新动作；具体任务调度从源码实现映射到本项目的手动额度查询，不单独创设另一套界面语义。
- 账号集合先筛选、分组和排序，再按每页 20/50/100 分页；默认 20，全选只作用当前页。
- 默认关闭窗口时询问最小化到托盘或退出；托盘内可以继续应用更新检查，但不能触发 Cursor 额度刷新。
- 更新设置与关于信息位于设置入口；更新提示、下载完成和更新后版本变化分别使用独立的全局反馈。

## 仍待确认

- 无。最终实施口径已封板，见 `docs/plans/2026-08-30-cockpit-accounts-redesign.md`。
